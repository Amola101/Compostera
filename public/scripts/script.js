function navbar() {
	var topnav = document.getElementById('topnav');

	if (topnav.className = "navbar") {
		topnav.className += " responsive";
	} else {
		topnav.className = "navbar"
	};
};


// cow move the file somewhere else :mad: don't add it with style files 